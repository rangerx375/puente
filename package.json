{
  "name": "puente",
  "private": true,
  "description": "Puente English workbook: static front end plus one Vercel function backed by Postgres.",
  "scripts": {
    "dev": "node scripts/dev.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
